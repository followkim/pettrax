<h1>Edit <%= link_to @animal.name, @animal %></h1>

<%= render "form", animal: @animal %>
<%= button_to "Delete", @animal, method: :delete, data: { turbo_confirm: "Are you sure?" } %>
<%= link_to "Cancel", @animal %>
